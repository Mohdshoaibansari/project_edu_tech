# Notification Module — Permissions

Notifications are auto-delivered by event subscribers. No user-triggered create endpoint.

| Permission | Purpose | Required Roles |
|-----------|---------|---------------|
| *(inherited)* | In-app notifications are read alongside any module access | All authenticated users |
